#pragma once


// Variables for dino's movement
bool isJumping = false; // Track if the dino is in the air
float verticalSpeed = 1.0f; // Dino's vertical speed (affected by gravity)

// Variables for cactus movement
float cactusSpeed = 3;//0.3f;

// Game state
bool isGameOver = false;